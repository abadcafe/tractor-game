{
  "tasks": {
    "check": "deno check frontend/*.ts frontend/**/*.ts",
    "build": "rm -rf static && mkdir -p static && tsc --project frontend/tsconfig.json && cp frontend/index.html static/ && cp frontend/style.css static/",
    "test": "deno test frontend/tests/"
  },
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "deno.ns"]
  },
  "fmt": { "lineWidth": 100 },
  "lint": { "rules": { "tags": ["recommended"] } }
}
