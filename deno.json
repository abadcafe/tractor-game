{
  "tasks": {
    "check": "deno check frontend/*.ts frontend/**/*.ts",
    "build": "rm -rf static && mkdir -p static && deno bundle -o static/main.js frontend/main.ts && cp frontend/index.html static/ && cp frontend/style.css static/",
    "test": "deno test frontend/tests/"
  },
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "deno.ns"]
  },
  "fmt": { "lineWidth": 100 },
  "lint": { "rules": { "tags": ["recommended"] } }
}
