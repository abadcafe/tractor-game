"""Shared policy-model domain.

This package owns observation encoding, legal semantic actions, the
policy/value network, checkpoint inference loading, and inference
execution.  It is independent of the training application.
"""

from __future__ import annotations
